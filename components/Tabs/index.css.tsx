import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  filtersContainer: {
    flexDirection: 'row',
    gap: 10,
    padding: 10,
  },
  filter: {
    fontSize: 16,
    paddingVertical: 4,
    paddingHorizontal: 4,
    textAlign: 'center',
    minWidth: 85,
  },
});
